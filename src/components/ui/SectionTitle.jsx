const SectionTitle = ({ text }) => {
  return (
    <div className="border-b border-neutral pb-4 mb-12">
      <h2 className="text-3xl font-medium capitalize">{text}</h2>
    </div>
  );
};

export default SectionTitle;
